import styled from "styled-components";
import { DefaultBlackColor } from "@Style/.";

export const Arrow = styled.button`
  position: absolute;
  left: 80%;
  top: 50%;
  font-size: 3rem;
  font-weight: bold;
  color: ${DefaultBlackColor};
`;
